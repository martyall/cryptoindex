# Prompts

Versioned prompt files. Filename carries the version (e.g. `gloss-v1.md`). The version string is part of the input hash for everything the prompt produces. Never edit a released version; add a new file.

Planned:
- `gloss-v1.md` (Phase 3): segmentation + gloss + terms + questions, JSON output.
- `agent-v1.md` (Phase 5): agent system prompt — cite verbatim, prefer abstention over guessing.
